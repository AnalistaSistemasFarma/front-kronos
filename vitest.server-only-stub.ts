/**
 * Stub de `server-only` para Vitest.
 * En Next.js el paquete real lanza si se importa desde un Client Component;
 * en tests (Node) necesitamos un no-op para poder ejercitar módulos server.
 */
export {};
